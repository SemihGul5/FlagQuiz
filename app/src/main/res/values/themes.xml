<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlagQuiz" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/appColor</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="Base.Theme.MainActivity" parent="Theme.MaterialComponents.Light.Bridge">
        <item name="colorPrimary">@color/appColor</item>
        <item name="titleTextColor">@color/white</item>

    </style>
    <style name="Base.Theme.Login" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <item name="colorPrimary">@color/appColor</item>
        <item name="titleTextColor">@color/white</item>

    </style>
    <style name="Base.Theme.HighScore" parent="Theme.MaterialComponents.Light.Bridge">
        <item name="colorPrimary">@color/appColor</item>
        <item name="titleTextColor">@color/white</item>

    </style>

    <style name="Theme.FlagQuiz" parent="Base.Theme.FlagQuiz" />
</resources>